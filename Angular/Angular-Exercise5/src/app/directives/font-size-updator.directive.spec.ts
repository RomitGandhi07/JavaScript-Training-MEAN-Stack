import { FontSizeUpdatorDirective } from './font-size-updator.directive';

describe('FontSizeUpdatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FontSizeUpdatorDirective();
    expect(directive).toBeTruthy();
  });
});
